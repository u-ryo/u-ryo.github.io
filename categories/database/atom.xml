<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom"><title><![CDATA[u-ryo's blog]]></title><link href="http://u-ryo.github.io//categories/database/atom.xml" rel="self"/><link href="http://u-ryo.github.io//"/><updated>2018-07-18T07:36:11+09:00</updated><id>http://u-ryo.github.io//</id><author><name><![CDATA[u-ryo]]></name></author><generator uri="http://sysgears.com/grain/">Grain</generator><entry><title type="html"><![CDATA[Unicode正規化とMySQLの照合順序は別物]]></title><link href="http://u-ryo.github.io//blog/2026/08/05/unicode正規化とmysqlの照合順序は別物/"/><updated>2026-08-05T16:38:38+09:00</updated><id>/blog/2026/08/05/unicode正規化とmysqlの照合順序は別物/</id><content type="html"><![CDATA[<p>データベースの検索仕様を考えていると、「正規化」という言葉が2つの意味で会議を飛び交うことがあります。RDB 設計の正規形 (第1〜第3正規形のあれ) と、Unicode の正規化 (NFC/NFD) です。名前が同じだけで完全に別物なんですけれど、話が混線したまま「DB がよしなにやってくれるのでは」という期待だけが残ることがあります。</p>
<p>結論から書きますと、MySQL は Unicode 正規化を「やってくれない」です。代わりに照合順序 (collation) が「何と何を同じとみなすか」を決めているんですけど、これは正規化の代替ではないですし、時々予想外のものまで同じにしてしまいます。手を動かして確かめたので、その記録、です。</p>
<h2>用語の整理</h2>
<ul>
<li><strong>RDB の正規化 (normalization)</strong>: テーブル設計から冗長性を取り除く設計論。今日の話には出てきません。</li>
<li><strong>Unicode の正規化 (normalization)</strong>: 同じ見た目の文字を同じバイト列に揃える変換。「ポ」は1文字 (U+30DD) でも「ホ + 結合半濁点」(U+30DB U+309A) でも表現でき、前者に揃えるのが NFC、後者に分解するのが NFD。macOS のファイル名経由で NFD の文字列が混入してくる、というのが実務での典型的な出会い方だと思います。</li>
<li><strong>照合順序 (collation)</strong>: DB が文字列を比較・整列するときの「一致と順序の定義」。</li>
</ul>
<h2>実験</h2>
<p>MySQL 8.0.46 (Docker公式image) で、NFC の「ポ」と NFD の「ポ」を各照合順序で比較してみます。文字列リテラルだとエディタやシェルが勝手に正規化しかねないので、バイト列を明示します。</p>
<figure class='code'><div class="highlight"><pre><span></span><span class="c1">-- NFC の「ポ」(U+30DD) と NFD の「ホ+結合半濁点」(U+30DB U+309A)</span>
<span class="k">SELECT</span><span class="w"> </span><span class="k">CHAR<em>LENGTH</span><span class="p">(</span><span class="k">CONVERT</span><span class="p">(</span><span class="n">X</span><span class="s1">&#39;E3839D&#39;</span><span class="w"> </span><span class="k">USING</span><span class="w"> </span><span class="n">utf8mb4</span><span class="p">))</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="n">nfc</em>char<em>len</span><span class="p">,</span>
<span class="w">       </span><span class="k">CHAR</em>LENGTH</span><span class="p">(</span><span class="k">CONVERT</span><span class="p">(</span><span class="n">X</span><span class="s1">&#39;E3839BE3829A&#39;</span><span class="w"> </span><span class="k">USING</span><span class="w"> </span><span class="n">utf8mb4</span><span class="p">))</span><span class="w"> </span><span class="k">AS</span><span class="w"> </span><span class="n">nfd<em>char</em>len</span><span class="p">;</span>
</pre></div></figure>
<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
</pre></td><td class='code'><pre><code class='html'><span class='line'>+--------------+--------------+
</span><span class='line'>| nfc_char_len | nfd_char_len |
</span><span class='line'>+--------------+--------------+
</span><span class='line'>|            1 |            2 |
</span><span class='line'>+--------------+--------------+
</span></code></pre></td></tr></table></div></figure>
<p>文字数からして違う、正真正銘の別文字列です。これを4つの照合順序で比較しますと:</p>
<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
</pre></td><td class='code'><pre><code class='sql'><span class='line'><span class="k">SELECT</span> <span class="k">CONVERT</span><span class="p">(</span><span class="n">X</span><span class="s1">&#39;E3839D&#39;</span> <span class="k">USING</span> <span class="n">utf8mb4</span><span class="p">)</span> <span class="o">=</span> <span class="k">CONVERT</span><span class="p">(</span><span class="n">X</span><span class="s1">&#39;E3839BE3829A&#39;</span> <span class="k">USING</span> <span class="n">utf8mb4</span><span class="p">)</span>
</span><span class='line'>       <span class="k">COLLATE</span> <span class="n">utf8mb4_bin</span>        <span class="k">AS</span> <span class="n">bin_eq</span><span class="p">;</span>        <span class="c1">-- 0</span>
</span><span class='line'><span class="k">SELECT</span> <span class="p">...</span> <span class="k">COLLATE</span> <span class="n">utf8mb4_general_ci</span> <span class="k">AS</span> <span class="n">general_ci_eq</span><span class="p">;</span> <span class="c1">-- 0</span>
</span><span class='line'><span class="k">SELECT</span> <span class="p">...</span> <span class="k">COLLATE</span> <span class="n">utf8mb4_unicode_ci</span> <span class="k">AS</span> <span class="n">unicode_ci_eq</span><span class="p">;</span> <span class="c1">-- 1</span>
</span><span class='line'><span class="k">SELECT</span> <span class="p">...</span> <span class="k">COLLATE</span> <span class="n">utf8mb4_0900_ai_ci</span> <span class="k">AS</span> <span class="n">ai_ci_eq</span><span class="p">;</span>      <span class="c1">-- 1</span>
</span><span class='line'><span class="k">SELECT</span> <span class="p">...</span> <span class="k">COLLATE</span> <span class="n">utf8mb4_0900_as_cs</span> <span class="k">AS</span> <span class="n">as_cs_eq</span><span class="p">;</span>      <span class="c1">-- 1</span>
</span></code></pre></td></tr></table></div></figure>
<table style="border-collapse:collapse">
  <tr><th style="border:1px solid #ccc;padding:4px 8px">照合順序</th><th style="border:1px solid #ccc;padding:4px 8px">NFC「ポ」= NFD「ポ」</th></tr>
  <tr><td style="border:1px solid #ccc;padding:4px 8px"><code>utf8mb4_bin</code></td><td style="border:1px solid #ccc;padding:4px 8px">一致しない</td></tr>
  <tr><td style="border:1px solid #ccc;padding:4px 8px"><code>utf8mb4_general_ci</code></td><td style="border:1px solid #ccc;padding:4px 8px">一致しない</td></tr>
  <tr><td style="border:1px solid #ccc;padding:4px 8px"><code>utf8mb4_unicode_ci</code></td><td style="border:1px solid #ccc;padding:4px 8px">一致する</td></tr>
  <tr><td style="border:1px solid #ccc;padding:4px 8px"><code>utf8mb4_0900_ai_ci</code></td><td style="border:1px solid #ccc;padding:4px 8px">一致する</td></tr>
  <tr><td style="border:1px solid #ccc;padding:4px 8px"><code>utf8mb4_0900_as_cs</code></td><td style="border:1px solid #ccc;padding:4px 8px">一致する</td></tr>
</table>
<p>Unicode Collation Algorithm (UCA) に基づく照合順序 (<code>unicode_ci</code>、0900系) は、正規化の違いを比較時に吸収します。アクセントも大文字小文字も区別する <code>utf8mb4_0900_as_cs</code> ですら一致させるのは少し意外でした —-- これは「正規化して等しいもの (正準等価) は等しい」という UCA の原則によるもので、accent-insensitivity とは別の話です。一方、旧世代の <code>utf8mb4_general_ci</code> は符号位置ごとの単純な重み比較なので吸収しません。</p>
<h2>では照合順序に任せてよいか</h2>
<p>よくない、です。照合順序は「一致の定義」を丸ごと入れ替えるダイヤルであって、正規化の違いだけを吸収する装置ではありません。MySQL 8.0 のデフォルトである <code>utf8mb4_0900_ai_ci</code> は accent-insensitive、つまり「アクセント記号を無視する」のですが、日本語では濁点・半濁点がアクセント扱いになります:</p>
<figure class='code'><div class="highlight"><pre><span></span><span class="k">SELECT</span><span class="w"> </span><span class="n"><em>utf8mb4</span><span class="s1">&#39;はは&#39;</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n"></em>utf8mb4</span><span class="s1">&#39;ぱぱ&#39;</span><span class="w"> </span><span class="k">COLLATE</span><span class="w"> </span><span class="n">utf8mb4<em>0900</em>ai<em>ci</span><span class="p">;</span><span class="w"> </span><span class="c1">-- 1</span>
<span class="k">SELECT</span><span class="w"> </span><span class="n"></em>utf8mb4</span><span class="s1">&#39;はは&#39;</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n"><em>utf8mb4</span><span class="s1">&#39;ばば&#39;</span><span class="w"> </span><span class="k">COLLATE</span><span class="w"> </span><span class="n">utf8mb4</em>0900<em>ai</em>ci</span><span class="p">;</span><span class="w"> </span><span class="c1">-- 1</span>
<span class="k">SELECT</span><span class="w"> </span><span class="n"><em>utf8mb4</span><span class="s1">&#39;が&#39;</span><span class="w">   </span><span class="o">=</span><span class="w"> </span><span class="n"></em>utf8mb4</span><span class="s1">&#39;か&#39;</span><span class="w">   </span><span class="k">COLLATE</span><span class="w"> </span><span class="n">utf8mb4<em>0900</em>ai<em>ci</span><span class="p">;</span><span class="w"> </span><span class="c1">-- 1</span>
<span class="k">SELECT</span><span class="w"> </span><span class="n"></em>utf8mb4</span><span class="s1">&#39;はは&#39;</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n"><em>utf8mb4</span><span class="s1">&#39;ばば&#39;</span><span class="w"> </span><span class="k">COLLATE</span><span class="w"> </span><span class="n">utf8mb4</em>0900<em>as</em>ci</span><span class="p">;</span><span class="w"> </span><span class="c1">-- 0</span>
</pre></div></figure>
<p>母 (はは) と父 (ぱぱ)、婆 (ばば) が一致します。デフォルトのまま日本語データに UNIQUE 制約や検索を載せると、この定義で「同じ」が判定されます。逆に <code>general_ci</code> はこれらを区別しますが、<code>'ABC' = 'abc'</code> は一致させます (case-insensitive)。つまり:</p>
<ul>
<li><code>general_ci</code>: NFC/NFD は別物扱い、大小文字は同一視</li>
<li><code>0900_ai_ci</code>: NFC/NFD は同一視、濁点も大小文字も同一視</li>
<li><code>0900_as_cs</code>: NFC/NFD は同一視、濁点も大小文字も区別</li>
</ul>
<p>「どの差を無視してどの差を区別するか」のセットを選んでいるのであって、単体で仕様の要求と一致することはまず期待できません。</p>
<h2>MySQLに正規化関数は無い</h2>
<p>ならば比較の前に正規化すればよいのですが、MySQLにはその関数がありません:</p>
<figure class='code'><div class="highlight"><pre><span></span><span class="k">SELECT</span><span class="w"> </span><span class="n">NORMALIZE</span><span class="p">(</span><span class="n">_utf8mb4</span><span class="s1">&#39;ポ&#39;</span><span class="p">);</span>
</pre></div></figure>
<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class='sql'><span class='line'><span class="n">ERROR</span> <span class="mi">1305</span> <span class="p">(</span><span class="mi">42000</span><span class="p">):</span> <span class="k">FUNCTION</span> <span class="n">mysql</span><span class="p">.</span><span class="n">NORMALIZE</span> <span class="n">does</span> <span class="k">not</span> <span class="n">exist</span>
</span></code></pre></td></tr></table></div></figure>
<p>PostgreSQL には 13 から <code>normalize()</code> があるので、これは MySQL 側の欠落です。つまり MySQL では、<strong>正規化はデータベースに入る前 (アプリケーション層) で行うしかありません</strong>。</p>
<h2>まとめ</h2>
<ul>
<li>Unicode 正規化と照合順序は別のレイヤーの仕組み。照合順序は正規化の代替にならない</li>
<li>保存前に NFC へ正規化する層をアプリケーション側に置く。DB には正規化済みの文字列だけを入れる</li>
<li>検索や UNIQUE 制約の「一致」が、使っている照合順序のどの定義に当たるのかを仕様として明文化する。デフォルト (<code>0900_ai_ci</code>) は日本語では「はは = ぱぱ」であることを知った上で選ぶ</li>
<li>検証環境: MySQL 8.0.46 (Docker)。この記事の SQL と出力は全て実行結果そのまま</li>
</ul>
]]></content></entry><entry><title type="html"><![CDATA[Adding a new column on JHipster]]></title><link href="http://u-ryo.github.io//blog/2018/11/24/adding-a-new-column-on-jhipster/"/><updated>2018-11-24T05:56:26+09:00</updated><id>/blog/2018/11/24/adding-a-new-column-on-jhipster/</id><content type="html"><![CDATA[<p>JHipsterでdatabaseのtableに新しいcolumnを足したいと思いました。
Liquibaseで管理されているので、直接DBを変えちゃうと立ち上がらなくなっちゃいます。
結構面倒臭いんですよねLiquibase。</p>
<p>基本的には<a href="https://www.jhipster.tech/development/#database-updates">Using JHipster in developmentのDatabase updates</a>に書いてある通り、です。
3通り紹介されていますが、最初に書かれている<code>entity sub-generator</code>を使った方が、testやi18n、angular側の面倒も見てくれるので楽です。
但し、generateされるjava/json/ts filesについては、面倒でも一つ一つdiffを見て上書きするか考えた方がいいです。場合によっては上書きしない方がいいcaseもありました(repository class、即ちDBを直接叩くclassとかresource class、即ちRest Controllerとか、i18nのglobal.jsonとか、独自に書いたところがあるものは特に)。</p>
<p>新規にtableごと加える時はこれでいいのかもしれません(以前やってうまく行った薄い記憶がありますがよく覚えてません)。
けれども、既存tableにcolumnを足す、となると、
<code>年月日_changelog.xml</code>を作ってくれないので、
いくら<code>gradle</code>かけてもDBのtableを変更してくれないんです。
更に、checksumが合わないといって起動しなくなります。
困ります。</p>
<p>なので、2番目の策を合わせて<code>changelog.xml</code>を作ってもらいます。
そのためには上記ページに書いてあるように、<strong>一旦buildしてから</strong>、
<code>gradle liquibaseDiffChangelog -PrunList=diffLog</code>すると、
class fileのdiffを取って<code>src/main/resources/config/liquibase/changelog/</code>以下にchangelogを作ってくれます。
それを<code>src/main/resources/config/liquibase/master.xml</code>に手で書き入れてからbuildすると、changelogが働いて<code>alter table</code>してくれます。
但しその際、元のtableのxml(<code>年月日_added_entity_テーブル名.xml</code>)をもとに戻しておかないと、「checksumが合わない」と言われるでしょう。</p>
<p>...というように、1番目と2番目の合わせ技的なところが最良のように思います。</p>
<p>どうしてもうまく行かない時、最後は以下の手で何とかなります。なりました。</p>
<ul>
<li>DBは自分で<code>alter table</code></li>
<li>checksumは<code>DATABASECHANGELOG.MD5CHECKSUM</code>にあるので自分で<code>update table set ...</code></li>
</ul>
]]></content></entry></feed>